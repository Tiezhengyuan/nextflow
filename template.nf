#!/usr/bin/env nextflow

/*
*
*
*/

workflow {
    main:

    emit:

}

process {
    debug true
    publishDir '', mode: 'copy', overWrite: true
    
    input:

    output:


    script:
    """
    """
}